{
  "name": "casa-pheli-web",
  "private": true,
  "type": "module",
  "description": "Statische site Casa Pheli — deploy vanuit deze map op Vercel.",
  "dependencies": {
    "@vercel/blob": "^2.3.3",
    "resend": "^6.12.2"
  },
  "scripts": {
    "build": "node -e \"import('fs/promises').then(fs=>fs.access('public/index.html')).then(()=>console.log('casa-pheli-web: build OK')).catch(e=>{console.error(e);process.exit(1)})\""
  }
}
